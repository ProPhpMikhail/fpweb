# Используем официальный образ с Node.js
FROM node:16

# Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем package.json и package-lock.json для установки зависимостей
COPY package*.json ./

# Устанавливаем зависимости
RUN npm install

# Копируем весь проект в контейнер
COPY . .

# Собираем проект Vue.js
RUN npm run build

# Открываем порт 8080 (по умолчанию для Vue.js приложения)
EXPOSE 8080

# Запускаем сервер для разработки (или продакшн-версию, в зависимости от окружения)
CMD ["npm", "run", "serve"]